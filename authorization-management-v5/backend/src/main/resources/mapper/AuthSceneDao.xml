<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auth.letter.dao.AuthSceneDao">

    <resultMap id="BaseResultMap" type="com.auth.letter.entity.AuthScene">
        <id column="id" property="id"/>
        <result column="letter_id" property="letterId"/>
        <result column="scene_name" property="sceneName"/>
        <result column="industry" property="industry"/>
        <result column="business_scene" property="businessScene"/>
        <result column="decision_level" property="decisionLevel"/>
        <result column="specific_rule" property="specificRule"/>
        <result column="rule_config" property="ruleConfig"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_time" property="updatedTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, letter_id, scene_name, industry, business_scene, decision_level,
        specific_rule, rule_config, created_by, created_time, updated_by, updated_time
    </sql>

    <select id="selectByLetterId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM auth_scene
        WHERE letter_id = #{letterId}
        ORDER BY created_time DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countByLetterId" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM auth_scene
        WHERE letter_id = #{letterId}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM auth_scene
        WHERE id = #{id}
    </select>

    <select id="selectByLetterIdForMatch" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM auth_scene
        WHERE letter_id = #{letterId}
    </select>

    <insert id="insert" parameterType="com.auth.letter.entity.AuthScene" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO auth_scene (
            letter_id, scene_name, industry, business_scene, decision_level,
            specific_rule, rule_config, created_by, created_time, updated_by, updated_time
        ) VALUES (
            #{letterId}, #{sceneName}, #{industry}, #{businessScene}, #{decisionLevel},
            #{specificRule}, #{ruleConfig}, #{createdBy}, #{createdTime}, #{updatedBy}, #{updatedTime}
        )
    </insert>

    <update id="update" parameterType="com.auth.letter.entity.AuthScene">
        UPDATE auth_scene
        <set>
            <if test="sceneName != null">scene_name = #{sceneName},</if>
            <if test="industry != null">industry = #{industry},</if>
            <if test="businessScene != null">business_scene = #{businessScene},</if>
            <if test="decisionLevel != null">decision_level = #{decisionLevel},</if>
            <if test="specificRule != null">specific_rule = #{specificRule},</if>
            <if test="ruleConfig != null">rule_config = #{ruleConfig},</if>
            updated_by = #{updatedBy},
            updated_time = #{updatedTime}
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM auth_scene WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM auth_scene
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>